1. Number of Requests: Typically, the free plan offers a limited number of OCR requests per month. This could range from a few hundred to a few thousand requests per month.

2. Number of Pages per Request: Each OCR request may be limited to a certain number of pages per request. For example, you might be able to process up to 10 pages per request.

3. Maximum File Size: There might be a limit on the maximum file size for each OCR request. This could range from a few megabytes to tens of megabytes.

4. Rate Limits: There might be rate limits on the number of requests you can make per minute or per hour. For example, you might be limited to a certain number of requests per minute to prevent abuse.

source venv/bin/activate